# 厨电制造专题大屏 · 设计文档

> 面向管理大屏的制造业（厨电行业）专题系列，沿用本仓库「公共资金专题」的三层范式
> （见 [`PublicFundsThemed.md`](PublicFundsThemed.md)）。本文是开发前的设计基线：
> 系列结构、虚构企业与数据设定、青橙配色公共样式、每页版式与指标勾稽、工程约定。
> 数据全部为编造的模拟数，每页挂「模拟数据」标识；真实口径由后端接口提供，本文不涉及。

---

## 1. 项目定位与范围

- **行业设定**：厨电制造集团，总部杭州，全国视角（多基地生产、全国仓网、全国门店 + 线上全渠道）。
- **规模**：首版 **21 块屏** = 1 门户 + 12 专题页（自动轮播）+ 8 二级详情子页，一次性全量交付；
  后经扩展批增至 **31 块** = 1 门户 + 17 专题页 + 13 子页（见 §7.5），
  再经全链路流转批增至 **32 块** = 1 门户 + 18 专题页 + 13 子页（见 §7.6），
  再经等距批（数字孪生 + 物联）增至 **35 块** = 1 门户 + 19 专题页 + 15 子页（见 §7.7）。
  下文 §1–§7 描述的是首版基线。
- **目录**：`screens/kitchen-appliance/`，页面互链为同目录相对路径；目录需同步加进
  `tools/build_index.py` 的 `CONTENT_DIRS`。
- **形态**：纯自包含单文件样板间（不套壳），1920×1080 舞台 + `fitStage()` 等比缩放，
  数据全内联，echarts 走 CDN。全部照抄 `CLAUDE.md` 样板间范式。

### 首版 21 块屏总表（扩展后的完整 31 块见 §7.5）

```
门户（1）
  └─ 经营驾驶舱：集团 KPI + 12 页导航宫格 + 产销存全景 + 经营健康雷达 + 风险清单
        │
专题页（12，按业务顺序串成自动轮播）
  ├─ 一期 · 供应与制造（4）  原料采购总览 / 供应商协同 / 生产制造总览 / 品质管理
  ├─ 二期 · 仓储与流通（4）  仓储库存 / 物流运输 / 门店零售 / 电商全渠道
  └─ 三期 · 营销与服务（4）  营销活动 / 售后服务 / 用户会员运营 / 经营健康指数（收官）
        │
子页（8，从专题页下钻进入）
  基地生产详情 / 供应商台账 / 库存库龄台账 / 物流线路详情 /
  省区门店分布 / 单品销售详情 / 售后工单台账 / 故障部件分析
```

### 三段式叙事骨架

| 段 | 回答的问题 | 视觉重心 |
|---|---|---|
| 一期 · 供应与制造 | 造不造得出、造得好不好 | 价格区间带、甘特、直通率漏斗，强调「过程」 |
| 二期 · 仓储与流通 | 存得多不多、运得快不快、卖到哪里 | 仓网地图、流向线、渠道桑基，强调「分布与流向」 |
| 三期 · 营销与服务 | 卖得动不动、用户满不满意、经营健不健康 | 转化漏斗、工单甘特、合成指数雷达，强调「转化与边界」 |

---

## 2. 虚构企业与数据设定

公开仓库，主体必须虚构：**「云麓厨电集团」**（虚构品牌，总部杭州）。
命名刻意不与任何真实厨电企业相近；页面不出现真实品牌、真实人名。

### 2.1 业务设定（所有页面共用的世界观）

- **六大产品线**：吸油烟机 / 燃气灶具 / 洗碗机 / 蒸烤一体机 / 集成灶 / 净水热水。
- **三大生产基地**：杭州临平（总部主基地：烟机、灶具）、安徽芜湖（洗碗机、蒸烤）、
  广东佛山（集成灶、净水热水）。另设「成都基地（在建）」作叙事彩蛋，不计产能。
- **七大区域仓（RDC）**：华东-杭州、华北-天津、华南-佛山、华中-武汉、西南-成都、
  东北-沈阳、西北-西安。基地→RDC 干线 + RDC→省区配送两级物流。
- **渠道**：线下门店（KA 卖场 / 品牌专卖店 / 建材集合店，覆盖 31 省）+
  线上全渠道（京东 / 天猫 / 抖音 / 苏宁易购 / 品牌自营，模拟平台一律用真实平台名之外可斟酌，
  开发时若避讳则改「平台A/B」式虚指——**默认用真实平台名**，属公开常识不涉脱敏）。
- **五大原材料**：冷轧板 / 不锈钢 / 铜材 / 面板玻璃 / 电子电控件。
- **供应商**：约 180 家，分战略（S）/ 优选（A）/ 合格（B）/ 观察（C）四级。

### 2.2 核心数字（当年度基准盘，全系列勾稽的单一数据源）

| 口径 | 数值 | 派生关系 |
|---|---|---|
| 年营收 | 112.6 亿元 | = 线下 48.4 + 线上 58.7 + 工程渠道 5.5 |
| 年销量 | 866 万台 | = 六产品线之和；单价×销量与营收对得上量级 |
| 年产量 | 902 万台 | = 三基地之和；产量 − 销量 ≈ 库存净增 + 在途 |
| 成品库存 | 118 万台 | 期初 + 产量 − 销量 = 期末，逐月滚动成立 |
| 门店数 | 3 240 家 | = 31 省之和 |
| 会员数 | 1 860 万 | 注册→绑定→活跃逐层递减 |
| 年售后工单 | 96.8 万单 | ≈ 保有量 × 年故障率，与「品质管理」页市场故障率同源 |
| 供应商 | 182 家 | = S 12 + A 45 + B 98 + C 27 |

**勾稽铁律**（延续仓库范式）：合计 = 分项之和；比率、达成率、占比一律 JS 现算不复制数字；
跨页共用的数字（产量、销量、库存、故障率、门店数）在各页取同一基准值，
月度序列可各页自编但年度合计必须回到上表。

---

## 3. 三层导航（照抄公共资金专题）

- **门户 → 专题页**：门户导航宫格 12 格（扩展后 17 格、全链路批 18 格，4×3 → 5×4），点击跳对应专题页。
- **专题页之间**：底部页码点 + 左右箭头 + 自动轮播（顺序即上表一期→三期），
  顶部「返回驾驶舱」链接回门户。
- **专题页 → 子页**：页内下钻入口（点图表元素或「查看详情」角标）跳同目录子页，
  子页顶部「返回上级」。子页归属：

| 子页 | 上级专题页 |
|---|---|
| 基地生产详情 | 生产制造总览 |
| 供应商台账 | 供应商协同 |
| 库存库龄台账 | 仓储库存 |
| 物流线路详情 | 物流运输 |
| 省区门店分布 | 门店零售 |
| 单品销售详情 | 电商全渠道（兼门店零售入口） |
| 售后工单台账 | 售后服务 |
| 故障部件分析 | 品质管理（兼售后服务入口） |

---

## 4. 视觉语言：青橙配色公共样式

### 4.1 气质定位

与公共资金专题的「藏蓝 + 金」区分：制造业用**深青黑底 + 工业青主色 + 工业橙强调**。
青 = 设备指示灯 / 精密仪器的冷静与秩序；橙 = 炉火、警示、产线活性。
橙同时承担「暖色告警 / 目标线 / 强调」三职，红色只留给真告警。

### 4.2 公共色板（系列级 token，`--mfg-*` 前缀）

配色是**系列公共样式**：全部 35 页使用同一组 `--mfg-*` 变量名与同一份 JS 镜像
`const PAL = {...}`，后续整套换色只改一处源、批量同步（见 4.3）。

| token | 值 | 用途 |
|---|---|---|
| `--mfg-bg` | `#051416` | 页面暗底（深青黑） |
| `--mfg-stage` | `#071b1e` | 舞台底 |
| `--mfg-panel` | `rgba(14,46,50,.55)` | 卡片面板底 |
| `--mfg-line` | `rgba(64,196,190,.22)` | 面板描边 / 分隔线 |
| `--mfg-cyan` | `#35e0d0` | 主色：主指标、主系列、高亮 |
| `--mfg-cyan-deep` | `#1a9e96` | 主色深阶：次系列、渐变下端 |
| `--mfg-blue` | `#4fa8e8` | 辅色：对比系列（保留一点蓝与全仓库呼应） |
| `--mfg-green` | `#7ee08a` | 正向：达成、合格、增长 |
| `--mfg-orange` | `#ff9d45` | 工业橙：强调、目标线、暖色叙事 |
| `--mfg-amber` | `#ffc94d` | 橙黄：次强调、库龄中档 |
| `--mfg-red` | `#ff5f6b` | 真告警：超限、失败、缺货 |
| `--mfg-violet` | `#a98cf5` | 补充系列色（桑基/旭日多分类时） |
| `--mfg-text` | `#d8f2ef` | 主文字 |
| `--mfg-text-dim` | `#7fa6a3` | 次文字 / 轴标签 |

分类序列色顺序：`cyan → orange → blue → green → amber → violet`（多系列图按此取色）。

### 4.3 换色机制（公共样式的落地方式）

页面保持自包含，但色板做成**可批量同步**：

- 单一色源：`shared/palette-manufacturing.json`（上表的 JSON 形式，另含 `_series` 分类序列色顺序）。
- 每页的 `:root{ --mfg-* }` 块与 `const PAL = {...}` 镜像都包在成对标记注释里，
  命名沿用仓库既有的 `xxx:begin / xxx:end` 惯例（同 `mockflag:begin`）：
  CSS 侧 `/* mfgpalette:begin */ … /* mfgpalette:end */`，
  JS 侧 `/* mfgpalette-js:begin */ … /* mfgpalette-js:end */`。
  `PAL` 之外另有一层手写的便利别名 `const C`（驼峰访问 + 衍生色），**不在同步块内**。
- 新增工具 `tools/sync_mfg_palette.py`：读色源 JSON，把 `screens/kitchen-appliance/`
  下所有页面标记区内的色板整体重写（幂等，可反复跑）。后续换整套配色 = 改 JSON + 跑脚本。
  `--remove` 只清块内容、保留标记本身（标记是页面结构的一部分，删了下次无处落笔）。
- 布局类 class/id 仍按每页唯一 2 字母前缀隔离，**只有色板 token 共用 `--mfg-` 前缀**。

### 4.4 其余视觉规范

字体、数字字形（翻牌缓动）、卡片圆角描边、扫描线/呼吸光带等活性动画、
CSS grid 模拟表格、徽章 `.xx-badge.is-xxx`——全部沿用公共资金专题规范，不再另立。

---

## 5. 每页设计规格

以下每页给出：回答的问题 / 版式 / 核心图表 / 关键指标与勾稽 / 下钻。
版式基线：左中右三栏或左右双栏，页头 KPI 带，页头正中留空给「模拟数据」标识。

### 门户 · 经营驾驶舱（前缀 `kd`）

- **问题**：集团今年经营怎么样，风险在哪，从哪看起。
- **版式**：顶部 6 枚集团 KPI（营收 / 销量 / 产量 / 库存周转 / 市场故障率 / NPS）；
  中部左「产销存全景」三联柱线（产量、销量、库存逐月，勾稽 2.2 平衡式），
  中「12 页导航宫格」（4×3，每格小图标 + 页名 + 一枚微型指标），
  右「经营健康雷达」（六维：交付/质量/库存/渠道/服务/增长，与收官页同源）；
  底部「关键风险清单」滚动条目（缺货 SKU、超期库龄、故障率抬头、物流延误线路）。
- **下钻**：宫格 → 12 专题页。

### 一期 · 供应与制造

**P1 原料采购总览（`yc`）**
- **问题**：大宗材料价格走势如何、采购成本可控吗、料够不够。
- **图表**：五大原材料价格三年月度走势 + 历史区间带（镜像「土地出让形势」的区间带做法）；
  采购额结构瀑布（按材料类）；库存覆盖天数五联温度计（安全线 = 橙色目标线）；
  采购降本达成子弹图。
- **勾稽**：采购额合计 = 五材料之和；覆盖天数 = 材料库存 ÷ 日均耗用（JS 现算）。

**P2 供应商协同（`gx`）**
- **问题**：供应商队伍质量如何、谁在拖后腿。
- **图表**：全国地图供应商分布（省级气泡，按级别着色）；
  准时交付率 × 来料合格率散点四象限（镜像「客户价值分层矩阵」，四色按 S/A/B/C）；
  齐套率月度走势；观察级（C 级）供应商预警表。
- **勾稽**：182 家 = 四级之和；四象限点数 = 有考核数据的供应商数。
- **下钻**：→ 供应商台账。

**P3 生产制造总览（`zz`）**
- **问题**：产得出来吗、计划达成没有、产线效率如何。
- **图表**：三基地产量动态排序柱（逐月回放）；六产品线计划达成子弹图；
  基地×产品线产能甘特（镜像「现金管理」14 期甘特，含今日线）；OEE 三联仪表（按基地）。
- **勾稽**：902 万台 = 三基地之和 = 六产品线之和（两个维度都收敛到同一总量）。
- **下钻**：→ 基地生产详情。

**P4 品质管理（`pz`）**
- **问题**：质量链每一段守住没有、缺陷集中在哪。
- **图表**：来料合格率 → 制程直通率 FPY → 出厂检验合格率 → 市场故障率 四段质量漏斗
  （本页招牌图，四段贯穿原料到售后）；缺陷类型帕累托（柱 + 累计占比线）；
  市场故障率 24 月走廊带（目标带内青色、越界橙/红）；六产品线 FPY 小多图。
- **勾稽**：市场故障率 × 保有量 ≈ 售后年工单量（与售后页同源）。
- **下钻**：→ 故障部件分析。

### 二期 · 仓储与流通

**P5 仓储库存（`ck`）**
- **问题**：存了多少、压在哪、周转健康吗。
- **图表**：全国地图七大 RDC 仓网（气泡 = 库存量，颜色 = 利用率档位）；
  库龄结构瀑布（0-30 / 31-60 / 61-90 / 90+ 天，90+ 用红）；
  库存周转天数 24 月走势 + 行业参考带；六产品线库存 × 动销率散点。
- **勾稽**：118 万台 = 七仓之和 = 四库龄段之和 = 六产品线之和。
- **下钻**：→ 库存库龄台账。

**P6 物流运输（`wl`）**
- **问题**：货在路上跑得顺不顺、时效达成没有。
- **图表**：全国地图两级流向线（基地→RDC 干线粗线、RDC→省区配送细线，lines 系列）；
  时效达成率日历热力（月视图）；干线 / 配送 / 尾程三段时长堆叠条；
  在途量 + 破损率双轴走势。
- **勾稽**：在途量与 P5 库存平衡式衔接；线路条数 = 3 基地×7 仓 + 7 仓×覆盖省区。
- **下钻**：→ 物流线路详情。

**P7 门店零售（`md`）**
- **问题**：线下卖得怎么样、哪些省强哪些省弱。
- **图表**：31 省销售额 choropleth（主图 + 南海小地图，见 §6）；
  同店增长 × 坪效省区象限散点（镜像「收入-增速象限」）；
  三渠道类型（KA / 专卖店 / 建材店）结构环形；门店数月度净增柱（开店正、闭店负）。
- **勾稽**：线下 48.4 亿 = 31 省之和 = 三渠道类型之和；3 240 家 = 省区之和。
- **下钻**：→ 省区门店分布。

**P8 电商全渠道（`ds`）**
- **问题**：线上盘子多大、哪个平台哪个品类在增长、直播转化如何。
- **图表**：渠道桑基（线上总盘 → 5 平台 → 6 产品线）；
  直播转化漏斗（曝光→进店→加购→成交）；平台×产品线增速热力矩阵；
  大促节奏日历热力（618 / 双 11 峰值叙事）。
- **勾稽**：线上 58.7 亿 = 5 平台之和 = 桑基各层守恒；与 P7 合计回到 2.2 渠道结构。
- **下钻**：→ 单品销售详情。

### 三期 · 营销与服务

**P9 营销活动（`yx`）**
- **问题**：投入产出划不划算、新品打得响不响。
- **图表**：活动 ROI 排序条（按 campaign，橙色基准线 = 盈亏线）；
  投放结构旭日（媒介→平台）；新品上市节奏时间轴 + 首销 30 天爬坡小多图；
  品牌声量 × 转化双轴走势。
- **勾稽**：营销费用合计 = 各 campaign 之和；新品销量计入 P7/P8 对应产品线。

**P10 售后服务（`sf`）**
- **问题**：工单接得住吗、修得快不快、用户满意吗。
- **图表**：工单流转甘特（受理→派单→上门→完结，含超时橙色段）；
  及时率三联仪表（24h 响应 / 48h 上门 / 一次修复）；
  故障类型旭日（产品线→部件，厨电部件层级天然适配）；满意度 24 月走势 + 投诉率。
- **勾稽**：96.8 万单 = 六产品线之和 ≈ 保有量×故障率（与 P4 同源）。
- **下钻**：→ 售后工单台账。

**P11 用户会员运营（`yh`）**
- **问题**：用户资产多大、活跃吗、复购吗。
- **图表**：会员漏斗（注册 1 860 万 → 绑定设备 → 年活跃 → 年复购）；
  会员等级金字塔 + 各级 ARPU；新增会员来源桑基（渠道→会员）；
  复购间隔分布直方 + NPS 仪表。
- **勾稽**：漏斗逐层递减；NPS 与门户 KPI 同值。

**P12 经营健康指数（`jk`，收官页）**
- **问题**：整体健康度多少分、哪个维度在失血。
- **图表**：加权合成大仪表（六维加权总分，直接镜像「资金运行指数」）；
  六分项雷达（交付/质量/库存/渠道/服务/增长，与门户雷达同源同值）；
  指数 24 月 K 线；加权明细可追溯表（每维的权重、原始指标、得分，JS 现算总分）。
- **勾稽**：总分 = Σ(分项 × 权重)，明细表可逐行验算；分项原始指标全部取自 P1–P11 的既有数字。

### 子页 8 块

| 子页（前缀） | 版式与内容 | 切换维度 |
|---|---|---|
| 基地生产详情（`jd`） | 左基地示意 + 产线状态灯板，右产量/OEE/直通率柱线；镜像「地市收入详情」的左右切换式 | 3 基地 |
| 供应商台账（`gy`） | 全量台账表（grid 模拟）：级别徽章、准时率、合格率、账期、年采购额，支持级别筛选 | S/A/B/C 级 |
| 库存库龄台账（`kn`） | 仓×产品线×库龄段嵌套表 + 右侧选中项库龄结构饼（镜像「分项目收入明细」左表右图联动） | 7 仓 |
| 物流线路详情（`xl`） | 左全国图高亮选中线路，右该线路时效/单量/成本台账 | 干线逐条 |
| 省区门店分布（`sq`） | 左省级地图（选中省下钻着色），右该省门店清单 + 城市结构 | 31 省 |
| 单品销售详情（`pd`） | 上单品 KPI + 价格带定位散点，下渠道×月度销售热力表 | 明星单品 8-10 个 |
| 售后工单台账（`gd`） | 工单全量表：状态徽章（待派/在途/完结/超时）、时效、满意度；顶部状态筛选 | 工单状态 |
| 故障部件分析（`bj`） | 左部件旭日下钻，右选中部件的故障率走势 + 批次追溯表（关联到基地与供应商——质量闭环叙事） | 部件类 |

---

## 6. 地图专题（全国视角）

- 全部复用 `shared/geo-data.js`，页面内联注册，无外网请求。内联**不手写**，由新增工具
  `tools/inline_geo.py <页面> china` 注入页面预留的 `/* geo:begin */ … /* geo:end */` 块
  （幂等，`--remove` 只清内容保留标记）。`china` 单个数据集约 465KB，人工处理不现实。
- 注意 `china` 是 **35 个 feature**：34 个省级 + 1 个空名要素（`adcode` 为 `100000_JD`，即九段线）。
- 全国图（P2 供应商、P5 仓网、P6 流向、P7 choropleth、`xl`/`sq` 子页）一律照抄
  「主图 + 右下角南海小地图」的既有拆分做法（纬度 17.5° 切分、双 geo 组件）。
- **`aspectScale: 0.75` 显式下发 + 运行时现算 `layoutSize`**，用
  `convertToPixel` 打极点验证不出画布——CLAUDE.md 里最坑的一条，每张地图页都过一遍。
- 流向线页（P6/`xl`）用 `lines` 系列 + `effect` 拖尾，起讫点坐标取省会/基地城市经纬度，
  内联一份小型城市坐标表即可，不需要新增 geo 数据。
- 杭州总部在 P2/P5/P6 地图上挂一枚橙色发光标（呼应「板块首位市加暖橙发光点」的既有手法）。

---

## 7. 工程约定

### 7.1 前缀对照表（已核对，全部未占用）

| 屏 | 前缀 | 屏 | 前缀 |
|---|---|---|---|
| 经营驾驶舱 | `kd` | 营销活动 | `yx` |
| 原料采购总览 | `yc` | 售后服务 | `sf` |
| 供应商协同 | `gx` | 用户会员运营 | `yh` |
| 生产制造总览 | `zz` | 经营健康指数 | `jk` |
| 品质管理 | `pz` | 基地生产详情 | `jd` |
| 仓储库存 | `ck` | 供应商台账 | `gy` |
| 物流运输 | `wl` | 库存库龄台账 | `kn` |
| 门店零售 | `md` | 物流线路详情 | `xl` |
| 电商全渠道 | `ds` | 省区门店分布 | `sq` |
| — | — | 单品销售详情 | `pd` |
| — | — | 售后工单台账 | `gd` |
| — | — | 故障部件分析 | `bj` |

（布局 class 用各页前缀；色板 token 统一 `--mfg-*`，见 §4.3。）

### 7.2 文件命名

`大屏样板间-厨电<主题名>1920.html`，如 `大屏样板间-厨电经营驾驶舱1920.html`、
`大屏样板间-厨电品质管理1920.html`。带「厨电」二字与既有银行/公共资金系列区分。

### 7.3 收尾同步清单（全量交付时一次做完）

1. `build_index.py` 的 `CONTENT_DIRS` 加 `screens/kitchen-appliance/`，重建索引，
   核对新分类分组与 count=1 特判命中（文件名含「样板间」，走列首规则）。
2. `python3 tools/inject_mock_flag.py` 注入模拟数据标识。
3. 新增 `shared/palette-manufacturing.json` + `tools/sync_mfg_palette.py`，全量跑一遍验证幂等。
4. README 总数、`docs/方案清单.md` 新增板块条目，手工同步。
5. Git 按仓库波次风格提交。

### 7.4 验证清单（每页）

- 控制台零报错；窗口缩放等比；翻牌缓动收敛后再核数字勾稽（中途值不算错）。
- 地图页跑极点 `convertToPixel` 验证；visualMap 分段设了 `text` 必补 `showLabel: true`。
- 图表容器父链全 flex 或显式高度（`flex:1` 高度坑）。
- 跨页勾稽抽查：产量/销量/库存/故障率/门店数回到 §2.2 基准表。

---

## 7.5 扩展批（P13–P17 + 5 子页，21 → 31 块）

首版 21 块交付后做过一次盘点：58 块成品大屏里 `bar` 用了 162 次、`line` 119 次、`pie` 66 次，
而 `charts/` 画廊里 12 类最有视觉冲击力的图表在成品屏**一次都没用过**。
扩展批的立意因此不是加题材，而是**让每一页都由「零使用图表」当主角**，
把视觉语言从「卡片 + 柱线」推开到液面流动 / 网络穿透 / 多轴刷选 / 三维地球四种。

| 页 | 前缀 | 主角图表（★ = 扩展前成品屏零使用） | 子页 | 前缀 |
|---|---|---|---|---|
| P13 研发与新品孵化 | `yf` | ★`tree` BOM 四层树、★`themeRiver` 投入演化、★`wordCloud` 需求热词 | 单项目研发详情 | `xf` |
| P14 工厂能碳中心 | `nt` | `sankey` 能流、★`liquidFill` 三水球、★`themeRiver` 24h 用能、★`boxplot` 单耗 | 单基地能耗详情 | `nh` |
| P15 设备与工艺参数 | `sg` | ★`parallel` 8 维工艺窗口（可刷选）、★`boxplot` 良率、★`graph` 设备网络 | 单产线设备详情 | `cx` |
| P16 海外出口与全球布局 | `hw` | ★`globe` 三维地球 + ★`lines3D` 航线、★`chord` 贸易流 | 单国家市场详情 | `gj` |
| P17 供应链风险图谱 | `fx` | ★`graph` 四层网络 + 风险传导高亮、★`chord` 供应流、★`parallel` 多维风险 | 供应商风险画像 | `hx` |

### 扩展批引入的两个外部插件（此前 21 页均为单一 CDN）

| 页 | 插件 | 说明 |
|---|---|---|
| P13 | `echarts-wordcloud@2.1.0` | `wordCloud` 不在核心里；已做插件缺失降级为 DOM 标签云 |
| P16 | `echarts-gl@2.0.9` | `globe`/`lines3D` 需要；**gl 官方 peer 依赖写的是 echarts 5.x，实测在 6.1.0 下可用**；WebGL 不可用时降级为 2D 世界地图 |
| P14 | `echarts-liquidfill@3.1.0` | `liquidFill` 不在核心里；已做降级为 CSS 液面卡 |

### 三维地球的自包含做法（P16）

`globe.baseTexture` **必须传原生 `canvas`**——实测传 ECharts 实例时不报错但**整个球体静默不渲染**。
所以贴图由内联世界 GeoJSON 用**等距圆柱投影**手绘到离屏 canvas：
`x=(lon+180)/360*W, y=(90-lat)/180*H`（正是 globe 贴图要求的 2:1 格式），
**真三维 + 零位图依赖 + 完全自包含**，不需要外链或 1–3MB 的 base64 贴图。
世界地图数据在 `shared/geo-world.js`（217 国 / 395KB，由 `tools/inline_geo.py` 注入，
数据集名 `world`）。⚠️ 国家名是英文且有坑：**美国是 `United States`**（不是 United States of America）、
**韩国是 `Korea`**（不是 South Korea），写错该国就静默不着色。

### ★ 出口口径与内销 112.6 亿的分离

P1–P12 与全部子页收敛的「年营收 112.6 亿 = 线下 48.4 + 线上 58.7 + 工程 5.5」是**内销口径**。
P16 引入出口时**没有改这个数**，而是作为独立口径并列：
**集团总营收 = 内销 112.6 亿 + 出口 26.30 亿 = 138.90 亿，出口占 18.93%**。
出口量 132.0 万台 = 自有产销结余 36（902 − 866）+ ODM 委外代工 96 万台，产能自洽。

### 导航规模变化

轮播 12 → **17 页**（停留 30 → **25 秒**，一轮约 7 分钟）；subnav **17 父页 / 13 有子页 / 4 无**；
drilldown **13 组**；门户宫格 4×3 → **5×4**（17 格，单格约 176×164px）。
`build_index.py` 无需改动——`THEME_ORDER` 已泛化，排序自动从 carousel/drilldown 的页表推导。

---

## 7.6 全链路流转批（P18，31 → 32 块）

参照等距插画风格的网络拓扑图（isometric network diagram），新增一块把整条价值链
画在一张图上的专题页 **「全链路流转」（前缀 `ql`）**，插在轮播「物流运输」之后
（仓储 → 物流 → 全链路 → 门店，流向叙事最顺）。

**主视觉：等距 2.5D 全链路流转图（纯 SVG，零新增依赖）。** 要点：

- 这类图的本质是**等距插画 + 拓扑连线**，不是 WebGL 3D——立体图标按 2:1 等距投影
  用三面 `<polygon>` 手绘（顶面提亮 16%、左面压暗 38%、右面压暗 56%，由 `PAL` 基准色
  现算，换色板后立体感自动跟随），标签是水平白底胶囊 + 虚线引线，文字不参与投影变形。
- **17 节点 / 48 段链路**：原料（182 家供应商）→ 三基地 → 七仓 → 四类出口
  （门店 / 电商 / 工程 / 海外自产）→ 终端用户 → 售后，售后再以橙色虚线逆向回流杭州基地。
  连线粗细 ∝ 流量，大流量链路跑 SMIL `animateMotion` 流动粒子；SVG 由 JS 从数据层生成。
- **流量逐段守恒**：产量 902 = 三基地（454/228/220，= 基地生产详情页产品线之和）
  = 七仓吞吐 = 内销 866 + 出口自产 36；基地→仓 21 条干线逐格取物流运输页 `RDCS.inb`、
  七仓库存逐仓取仓储库存页矩阵行和——**跨页数字不另编，一律引权威源**；
  逆向 12.4 万单 = 返厂 8.6 + 退换 3.8（工单 96.84 = 保有量 3227 × 故障率 3.0011%，
  与品质/售后页同源）；ODM 委外出口 96 万台不经自有仓网，图例注明不入图。
- 杭州总部基地挂橙色脉冲光环（复用「总部发光标」手法）；等距舞台、预警清单、
  事件滚动条都不依赖 echarts，资源守卫**之前**渲染——断网时主视觉照常活着。
- 侧栏 4 张 ECharts 小图：基地→仓网流量矩阵热力、七仓吞吐×周转双轴、
  渠道出口结构条形、逆向服务流环形。

导航规模：轮播 **18 页**（一轮 7.5 分钟）、subnav **18 父页 / 13 有子页 / 5 无**、
门户宫格 **18 格**（5×4，末 2 格留空）；drilldown 不变（本页无子页）。

---

## 7.7 等距批（P19 + 2 块数字孪生子页，32 → 35 块）

全链路流转页验收后，把等距 2.5D 范式推到三个尺度，形成「微观-中观-宏观」的完整叙事：
中观全链路（§7.6）之外，新增微观的厂区/仓内数字孪生与向外的设备物联网络。
三页复用 §7.6 沉淀的全部原语：`isoCube` 三面着色（由 `PAL` 基准色现算提亮/压暗，
换色板立体感自动跟随）、白底胶囊标签 + 虚线引线、SMIL `animateMotion` 流动粒子、
等距舞台不依赖 echarts（资源守卫之前渲染，断网时主视觉照常活着）。零新增外部依赖。

| 屏 | 前缀 | 定位 | 要点 |
|---|---|---|---|
| 智能物联运营（专题页 P19） | `wn` | 轮播插在「用户会员运营」之后 | 等距 IoT 拓扑：6 设备 → 网关 → 安全接入 → IoT 核心/数据中台/OTA → APP/大屏/工单。在网 = Σ 保有(售后页口径)×联网率；预警转单 21.3 万 = 售后工单 96.84 的现算占比；24h 消息三层堆叠做出「早午晚三峰 = 厨房节奏」的行业叙事 |
| 基地数字孪生（子页） | `jt` | 挂「生产制造总览」，与「基地生产详情」互为姊妹页（指标视角 ⇄ 空间视角，chip 互链带 hash） | 等距厂区工艺流 8 段：原料库→冲压→焊接→表面处理→A/B 装配→检测→包装→立库→月台；产线状态灯画在装配车间立面上（与生产详情页同一份产线表）；检测不良经返修站橙色虚线回流 B 区；工序日通过量 = 日产 × 确定性损耗系数逐段收敛；按 3 基地切换 + 成都在建彩蛋 tab |
| 智慧仓数字孪生（子页） | `cl` | 挂「仓储库存」，与「库存库龄台账」互为姊妹页 | 等距仓内布局：月台→暂存→立体库巷道群→AGV→分拣→包装→出货；**货架亮格比例 = 库容利用率**、巷道数与 AGV 车队规模 ∝ 库容——七个仓长得各不相同但同一套代码；吞吐/库存/库容/配送逐仓取物流运输页 `RDCS.inb`、仓储库存页矩阵行和与 `WH.cap`；按 7 仓切换 |

导航规模：轮播 **19 页**（一轮约 8 分钟）、subnav **19 父页 / 13 有子页 / 6 无**、
门户宫格 **19 格**（5×4，末 1 格留空）、子页 **15 块**；drilldown 不变
（两块孪生子页经姊妹页 chip 与门户进入，不占用父页的单一 subnav 位）。

---

## 8. 交付节奏（内部构建顺序）

一次性全量交付，但构建按依赖排序：

1. **公共件先行**：色板 JSON + 同步脚本 + 门户（定调视觉与导航骨架）。
2. **一期 4 页 → 二期 4 页 → 三期 4 页**（专题页轮播按此顺序接线）。
3. **子页 8 块**（依赖上级页的切换范式与数据）。
4. 收尾同步清单（§7.3）+ 全量目检。
