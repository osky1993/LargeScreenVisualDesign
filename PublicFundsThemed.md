# 公共资金专题大屏 · 设计与实现说明

> 面向接手这套专题的 **UI 设计** 与 **前端开发**。本文自包含，不需要先读仓库里的其他文档。
>
> 现有产物是 27 个可双击打开的自包含 HTML（1920×1080 成品大屏），无构建系统、无 npm 依赖，
> 运行时只从 CDN 加载 ECharts 6。当前版本的数据是内联的模拟数据；**真实数据由后端接口提供，
> 数值本身的准确性由后端与数据开发保障**，本文不涉及口径核算，只讲设计理念、视觉规范、
> 实现范式，以及接口化改造时前端要注意的地方。
>
> 每页顶边正中挂一枚橙色「模拟数据」标识（`tools/inject_mock_flag.py` 注入），
> 提示页面上的数字都是编的，不要当成真实业务数据。接上真实接口后用
> `python3 tools/inject_mock_flag.py --remove` 整批摘掉。

---

## 目录

1. [系列全景](#1-系列全景)
2. [设计理念](#2-设计理念)
3. [视觉语言](#3-视觉语言)
4. [页面骨架与实现范式](#4-页面骨架与实现范式)
5. [三层导航架构](#5-三层导航架构)
6. [子页版式原型与选型](#6-子页版式原型与选型)
7. [地图专题](#7-地图专题)
8. [对接真实接口时的注意事项](#8-对接真实接口时的注意事项)
9. [已定型的决策与理由](#9-已定型的决策与理由别改回去清单)
10. [踩坑记录](#10-踩坑记录)
11. [扩展指南](#11-扩展指南)
12. [验证清单](#12-验证清单)
13. [附录：27 块屏与 CSS 前缀对照](#13-附录27-块屏与-css-前缀对照)

---

## 1. 系列全景

27 块屏分三层：

```
门户（1 块）
  └─ 专题总览：系列级 KPI + 13 页导航宫格 + 四本预算全景 + 资金运行指数雷达 + 风险清单
        │
专题页（13 块，按业务顺序串成自动轮播）
  ├─ 一期 · 收入主线（4）  全国收入总览 / 全国分地区收入 / 江苏地市收入 / 收入分项目
  ├─ 二期 · 结构专题（4）  重点城市及县区 / 税收分行业 / 新动能税收 / 土地出让形势
  └─ 三期 · 资金运行（5）  国资社保预算 / 现金管理 / 资金运行指数 / 政府债券发行 / 资金余额分析
        │
子页（13 块，从专题页下钻进入）
  按对象切换的详情页 + 全量台账页 + 地图分布页
```

### 三段式叙事骨架

13 块专题页不是平铺的功能页，而是一条有起承转合的叙事线：

| 段 | 回答的问题 | 视觉重心 |
|---|---|---|
| 一期 · 收入主线 | 钱有多少、从哪来、谁贡献的 | 地图与桑基，强调"分布"与"流向" |
| 二期 · 结构专题 | 结构好不好、动能在哪、哪块在承压 | 树图、标签墙、对比带，强调"结构"与"分化" |
| 三期 · 资金运行 | 钱怎么管、风险在哪 | 甘特、走廊带、仪表与预警清单，强调"过程"与"边界" |

**土地出让形势**是全系列唯一使用暖橙告警配色的整页下行叙事；**资金余额分析**是收官预警页。
这两页的色调偏离主色系是**刻意的**，用于在轮播中形成情绪节奏，不要"统一"掉。

### 每页的组织法：一个主视觉 + 若干佐证

每块屏都遵循"中央一块主视觉面板（占最大面积），四周若干佐证面板"的结构。
主视觉承担该页的核心论点，佐证面板从不同侧面支撑它。判断一块屏做得对不对，
最简单的方法是问：**遮住所有佐证面板，只看主视觉，这一页想说的话还成立吗？**

---

## 2. 设计理念

### 2.1 这是常驻大屏，不是交互报表

所有设计取舍的第一前提：屏幕挂在墙上长期运行，**没有人操作它**。由此推出：

- **信息必须自解释**：不能依赖 hover 才能看到关键数字。tooltip 只承载补充信息，
  主要读数一律直接标在图上或写在面板里。
- **必须有"活性"**：静止画面会被误认为死机。每块屏都用 `setInterval` 驱动轮播/高亮/翻牌，
  用 CSS `@keyframes` 驱动扫描线、呼吸光点。但活性**不能干扰读数**——见 §9 关于翻牌缓动的决策。
- **不做 teardown**：页面长期运行，定时器不清理是刻意的；相应地必须保证内存不增长
  （图表只 `init` 一次，切换只 `setOption`）。
- **异常要看得见**：每页预置一条隐藏的告警条，资源加载失败或自检不通过时点亮，
  而不是白屏或静默出错。

### 2.2 一屏之内不翻页、不滚动

1920×1080 是固定画布，所有内容必须在一屏内放下。这意味着：

- 表格行数是**设计约束**，不是运行时才发现的问题。设计阶段就要按最长的那一档排版
  （比如四大板块里西部有 12 个省，三大板块里苏南有 5 个市）。
- 行高一般写成 `min(固定值, 可用高度 ÷ 行数)`，行少时留出的余量交给一块补白内容
  （占比条、特征卡），行多时补白自动隐藏。
- **绝不用 `scrollTop` 做滚动**。需要滚动展示的长列表用"两份副本 + `translateY` 步进"
  做无缝滚动。

### 2.3 口径显式化

财政数据的歧义主要来自口径。每页页头固定写明**口径 / 区间 / 层级**三件事，例如
「口径 地方一般公共预算收入 | 区间 2026 年 1–7 月累计 | 全国 31 省 · 四大板块」。
每个面板头右侧再写该面板自己的口径补充（着色依据、分档方式、基准线含义）。

这不是装饰。轮播状态下观众随时切进来，没有上下文，口径标注是唯一的锚。

### 2.4 层级对不齐时，宁可粗，不可编

做下钻时反复遇到"数据层级比可视层级粗"的情况。**统一原则：按已有的粗口径整体呈现，
并在界面上写明，绝不为了填满画面去编造更细的数。** 具体案例见 §7.5 和 §9。

---

## 3. 视觉语言

### 3.1 色彩语义体系

色彩在这套系列里**承载语义**，不是随机分配的装饰。同一个颜色在不同页面表达同一类含义：

| 色值 | 语义 |
|---|---|
| `#f0c060` / `#ffdc8f` 金 | 合计口径、本省（江苏）、东部板块、苏南板块、第一/首位、当前选中 |
| `#39d0d8` 青 | 非税收入、中部板块、苏中板块、次级强调、下钻入口 |
| `#3d9cff` 蓝 | 西部板块、苏北板块、常规数据、上年同期 |
| `#33e0a6` 绿 | 正增长、健康区间、达成 |
| `#ff5f6e` 红 | 负增长、预警区间、超限 |
| `#ff9d4d` 暖橙 | 东北板块、下行叙事（土地出让整页）、重点标记点 |
| `#08203f` / `#04101f` | 面板底 / 页面底 |

设计与开发的**交接点**在这里：色值同时存在于两处——CSS 里的 `:root { --xx-gold: … }`
和 JS 里的 `const C = { gold: "…" }`。因为 ECharts 的 option 里读 CSS 变量不方便，
所以维护了一份硬编码镜像。**改配色必须两处同步改**，只改一处会出现"面板边框变了、图表没变"。

### 3.2 舞台与栅格

- 画布固定 **1920×1080**，深底 `#04101f`，舞台面 `#08203f` 加一层极淡的网格与径向渐变。
- 主体是三列或左右两列栅格，列间距 14px，页面内边距 12~18px。
- 纵向自上而下：**页头（84px 左右）→ KPI 指标带（96~132px）→ 主体区（自适应）→ 底部跑马灯（52px）**。
- 页面高度必须**逐块相加精确等于 1080**。这是硬性要求，设计稿要给出每一块的像素高度。

### 3.3 面板范式

每个面板头是固定的四段式结构，从左到右：

```
▍ 中文标题        ENGLISH SUBTITLE                        口径补充说明
（金色竖条）      （淡色小字英文）                          （右对齐，说明本面板的着色/分档/基准）
```

面板本体的常用形态：
- **图表面板**：一个 ECharts 容器占满剩余高度
- **表格面板**：表头 + 行列表 + 表尾合计，全部用 CSS grid 模拟（见 §4.5）
- **混合面板**：图表 + 下方一条紧凑数据带

### 3.4 KPI 指标带

页头下方一排 4~5 张 KPI 卡，每张的结构是：**图标 + 标签 → 大数字 + 单位 → 一行副说明**。
大数字用等宽数字字体（`font-variant-numeric: tabular-nums`），避免翻牌时宽度跳动。

### 3.5 状态徽章与正负着色

- 正负数一律带符号并着色：绿 `+3.5%` / 红 `−2.4%`
- 状态徽章用 `.xx-badge.is-xxx` 的形式，三档配色对应 健康（绿）/ 关注（金）/ 预警（红）
- 表格行的负值另加一条淡红色左侧标记，让整行在扫视时可辨

### 3.6 动效节奏

| 用途 | 周期 | 说明 |
|---|---|---|
| 专题页轮播 | 30 秒 | 可配置，页头右上有上一页/下一页/暂停按钮 |
| 子页对象切换 | 10~15 秒 | 鼠标进入关键面板时暂停 |
| 图内高亮巡游 | 3~4 秒 | 地图省份/城市依次高亮 |
| 翻牌缓动 | 700ms | 固定时长，见 §9 |
| 呼吸/扫描/流光 | CSS 动画 | 1.6~6 秒不等，纯装饰 |

---

## 4. 页面骨架与实现范式

### 4.1 固定舞台 + 等比缩放

页面是一个固定 1920×1080 的 `.xx-stage`，绝对定位居中，靠 CSS transform 缩放适配窗口：

```
stage 定位：position:absolute; left:50%; top:50%; transform:translate(-50%,-50%) scale(s)
缩放比例：s = Math.min(innerWidth / 1920, innerHeight / 1080)
时机：首次执行一次，并监听 window resize
```

**由此推出一条重要约束：整台靠 CSS transform 缩放，图表容器的像素尺寸恒定不变，
所以 ECharts 实例 `init` 之后永远不需要、也不应该调用 `chart.resize()`。**
全系列 `resize()` 调用数为 0。看到有人加 `resize` 就是理解错了缩放机制。

### 4.2 CSS 前缀隔离

每块屏分配一个唯一的两字母前缀，所有 class / id / CSS 变量 / keyframes 都带该前缀
（如 `gs-panel`、`--gs-gold`、`gsDot`）。状态修饰符是唯一例外，统一用 `is-*`
（`is-visible` / `is-focus` / `is-dim` / `is-on`）。

这样做的原因：这些屏可能被嵌进同一个宿主页面或 iframe 集合，前缀隔离保证互不污染。
新建页面前先确认前缀没被占用。

### 4.3 资源守卫

每页 DOM 里预置一条隐藏的告警条，脚本开头先判断依赖是否就绪：

```
若 window.echarts 不存在 → 点亮告警条并直接 return，不再执行后续逻辑
```

自检失败（数据结构异常、层级对不上）时同样点亮这条告警条，并写明失败原因。
这条告警条是常驻大屏唯一的错误出口。

### 4.4 自包含与外链纪律

每个 HTML 是完整自包含的单文件：CSS 内联、JS 内联、GeoJSON 内联。
**全文只允许一处外链**——ECharts 的 CDN 地址。这条纪律的意义是：
文件可以直接双击打开、可以拷进任何环境、不依赖构建，也便于审计。

### 4.5 表格一律用 CSS grid，不用 `<table>`

表头是一个 grid 容器定义列宽，行列表用 `<ul><li>` 生成并共用同一套列宽。
好处是行高、斑马纹、徽章、进度条都能自由排布，且列宽在表头表尾之间天然对齐。

### 4.6 图表容器的高度陷阱

ECharts 容器常写成 `flex: 1; min-height: 0`，但 **`flex:1` 只在直接父级也是 flex 容器时才拿得到高度**。
如果图表 div 外面套了一层非 flex 的 wrapper，会算出高度 0、图完全不渲染。
保证图表容器的父链上每一级要么是 flex 容器，要么有显式高度。

**更隐蔽的一条**（本系列栽过四次）：如果图表容器有一个 `flex:none` 的兄弟节点是
JS 动态插入的，而 `echarts.init` 在插入之前就执行了，画布会锁死在"兄弟节点还不存在"
时的那个偏大的高度；又因为本系列从不 `resize()`，这个错误高度会一直存在，
图表溢出盖住下方内容。**规则：所有静态兄弟节点必须先于 `echarts.init` 落地。**

---

## 5. 三层导航架构

### 5.1 四种入口

| 层级 | 形态 | 说明 |
|---|---|---|
| 门户 → 专题页 | 导航宫格卡片 | 13 张卡，可点击跳转；有子页的卡片带"可下钻"角标 |
| 专题页 ↔ 专题页 | 自动轮播 | 按业务顺序循环，页头右上带 上一页 / 下一页 / 暂停 |
| 专题页 → 子页 | 三种下钻 | 行级 / 画布级 / 面板级，见下 |
| 子页内部 | 对象切换器 | 上一个 / 下一个 / 直接点选，配合自动轮换 |

### 5.2 三种下钻入口的适用场景

- **行级下钻**：表格行、卡片、图例行、标签墙。悬停时整行高亮并出现 ↗ 角标，点击进入子页并定位到该对象。适用于"子页是按对象切换的详情页"。
- **画布级下钻**：树图分块、桑基节点、环形图扇区、小多图子图。这些图形不是 DOM，只能绑定 ECharts 实例的 click 事件。
- **面板级入口**：当子页是**全量台账、没有切换对象**时（如债券逐笔明细、土地逐宗成交），不做行级下钻，改在相关面板头挂一枚直达 chip。

### 5.3 页头的子页导航标签

13 块专题页页头右上统一有一枚标签，直接写明**本页有没有二级子页、子页叫什么、页内入口在哪**。
有子页的是青色可点的链接，没有的显示"本页无"。这是为了让观众在轮播中知道哪些页还能往下看。

### 5.4 hash 传参约定

下钻通过 URL hash 传参：`<子页文件>#<对象名>`，对象名做 URL 编码。子页读 hash 后直接定位到该对象。

**两条硬性约定**：
- 子页内切换对象时用 `history.replaceState` 同步 hash，**绝不用 `location.hash = …`**
  ——后者会触发跳转和滚动，在常驻大屏上表现为画面抖动。
- 图上显示的可能是简称而 hash 需要全称（例如桑基图上写"国有资源有偿使用"，
  子页认的是"国有资源（资产）有偿使用收入"），所以匹配表要把"图上名"和"跳转名"分开维护。

### 5.5 注入脚本的工作方式

轮播、下钻入口、子页标签这三样**不写在页面源码里**，而是由脚本注入。每个脚本：

- **配置驱动**：新增一个页面/入口只改脚本里的配置表，不动页面源码
- **幂等**：可反复运行，用成对的注释标记（如 `<!-- drill:begin -->` … `<!-- drill:end -->`）划定自己的地盘，重跑时先清除旧块再写入新块
- **可撤销**：都支持 `--remove` 还原
- **互不干扰**：每个脚本只清理自己的标记块

注入的下钻脚本独立于页面主脚本运行，并用 `MutationObserver` 持续重新标记——
因为专题页的表格和卡片是定时轮播重绘的，一次性标记会被冲掉。

**一个已知的工程细节**：多个注入脚本都往 `</style>` 之前插入自己的 CSS 块，
若不钉死相对位置，换个运行顺序就会产生"增删行数相同、内容完全一样"的纯顺序假 diff。
现在的做法是让后注入的块固定插在前一个块之前。

---

## 6. 子页版式原型与选型

### 6.1 四种原型

| 原型 | 结构 | 适用数据形态 |
|---|---|---|
| **① 左表右图** | 左侧明细表约 55~62%，右侧图表 | 对象有稳定的分项清单，且分项之间要比大小、看占比 |
| **② 上指标 + 中下柱线** | 顶部 KPI 带，中部与下部柱线混合图 | 对象的核心是时间序列与达成情况，分项较少 |
| **③ 全页表格 / 左右双表** | 整页大表，或左右两张对照表 | 全量台账，逐笔逐宗，没有切换对象 |
| **④ 其他（图为主）** | 地图、时间线、嵌套表 + 联动图等 | 空间分布、单据全流程、层级结构 |

### 6.2 选型的判断顺序

1. **子页有没有"切换对象"？** 没有（是全量台账）→ 原型③，入口用面板级 chip。
2. **对象的分项是清单式的吗？** 是 → 原型①，左表右图。
3. **对象的核心是时间演进吗？** 是 → 原型②。
4. **有空间维度或层级结构吗？** 有 → 原型④，用地图或嵌套结构。

### 6.3 13 块子页的实际选型

| 子页 | 原型 | 要点 |
|---|---|---|
| 地市收入详情 | ④ 地图 + 图 | 左侧从表格改成了该市的**县区地图**，更直观 |
| 三大板块地市分布 | ④ 一页三栏 | 苏南/苏中/苏北 三个带识别色边框的独立区块并列 |
| 四大板块分省明细 | ① 左图右表 | 左全国图按四色着色，右为该板块分省台账 |
| 分项目收入明细 | ④ 嵌套表 + 饼 | 左三级可展开嵌套表，右饼图随选中层级切子项占比 |
| 主体税种月度走势 | ④ 折线为主 | 6 条曲线叠加同一坐标系，带四种视图切换 |
| 行业税收详情 | ① 左表右图 | 切换 13 个行业门类 |
| 重点县区详情 | ② 上指标 + 柱线 | 切换 9 个百亿县区 |
| 资金余额预警详情 | ② 上指标 + 柱线 | 切换 5 个关注地区 |
| 新动能四大类 / 赛道地市分布 | ④ 地图热力 | 江苏 13 市地图叠热力 |
| 债券发行台账 | ③ 全页大表 | 逐笔明细 |
| 土地成交台账 | ③ 左右双表 | 左成交右流拍，延续父页暖橙配色 |
| 现金操作详情 | ④ 时间线 | 单期操作全流程 |

---

## 7. 地图专题

这套系列有十余块屏带地图，是坑最集中的部分，而且**多数错误不报错、只是画错**，
必须靠主动探测发现。

### 7.1 四种边界粒度

| 粒度 | 用途 |
|---|---|
| 全国省级（含港澳台与南海） | 全国分地区收入、四大板块分省明细 |
| 江苏 13 个设区市 | 地市收入、新动能地市分布 |
| 苏南 / 苏中 / 苏北 三板块 | 板块对照 |
| **江苏 95 个县区**（区 / 县级市 / 县） | 地市详情页的县区地图 |

边界数据由脚本从公开地理服务抓取后**内联进页面**，页面本身不联网取图。
县区级数据量大，抓取后做了拓扑保持简化（容差约 100 米，在市域出图比例下不可见），
体积从约 650 KB 压到 363 KB。

### 7.2 ★ 最坑的一条：`aspectScale` 导致的静默裁切

ECharts 的 `geo` 与 `series-map` **默认带 `aspectScale: 0.75`**，即经度方向被压缩到 3/4。
因此：

```
有效宽高比 = 经度跨度 × 0.75 ÷ 纬度跨度        ← 不是经纬跨度之比
```

全国图的真实值是约 1.30，而按经纬比算会得到 1.74。而 `layoutSize` 只作用在**较长的那条边**上，
所以按经纬比估算会高估宽度，反推出的高度超出画布，**地图上下被静默裁掉——没有任何报错**。

实测过的一次事故：漠河被画到 y = −40、三亚在 y = 441，而画布高度只有 402，
海南整个跑到画布外，北边一排省份的顶是平切的。

**规避方法**：
1. `layoutSize` 按容器尺寸与真实包围盒**运行时现算**，不要写死；
2. 把同一个 `aspectScale` 显式下发给 geo 组件与 series，不依赖默认值；
3. **验证手段**：用 `chart.convertToPixel({geoIndex:0}, [经度, 纬度])` 打几个极点
   （如漠河 122.5/53.5、喀什 75.9/39.5、三亚 109.5/18.25、台北 121.5/25.05），
   确认返回的像素坐标全部落在 `[0, 容器宽] × [0, 容器高]` 内。

这条对市域图同样成立：江苏 13 个市的有效宽高比从南京的 0.475 到徐州的 1.371，跨度极大，
给同一个 `layoutSize` 必然有市被压扁或被裁。

### 7.3 全国图必须做"主图 + 南海小地图"

直接把全国边界注册后画出来会被压扁，因为纬度下界是 3.4° 而不是大陆的 18.1°。原因有两个，
**两个都要处理**：

1. 数据里有一个**空名要素**（行政编码带 `_JD` 后缀）是九段线；
2. **海南省要素自身包含三沙群岛**——133 个多边形里有 129 个是低纬度的岛点。

只删九段线不够，海南自身就会把纬度拉到 3.82°。正确做法是按纬度 **17.5°** 为界把这两部分
切到独立的要素集合另行注册，做成国标常见的「主图 + 右下角南海小地图」：

- 主图包围盒回到 18.1°~53.6°，宽高比从 1.24 回到 1.30，可以显著放大；
- 小地图用第二个 geo 组件，`zlevel` 抬高以压住边框图形；它自身的包围盒就是全部内容，不会溢出小框；
- 小地图**紧贴主图右下、与主图底对齐**——两张图必须读得出是同一张全国图，
  离得太远会被误认为是两张无关的图。

### 7.4 不要用 `visualMap`

两个原因：

1. 分段式 `visualMap` 如果没有给 `outOfRange`，会在渲染阶段抛
   `Cannot read properties of undefined (reading 'coord')`，而控制台只显示**无栈的 `Script error.`**，极难定位；
2. 分段式一旦设置了两端文字，`showLabel` 默认变成 `false`，**每一档的标签会被整体吞掉**，
   图例只剩色块。

**统一做法：着色直接写在数据项的 `itemStyle.areaColor` 上，图例用 DOM 自己画。**
这样还有个附带好处——图例可以承载"本期无"这类状态文案。

### 7.5 数据层级比地图层级粗时怎么办

做地市详情页的县区地图时遇到的典型情况：13 个市里只有南京拆到了区，其余 12 个市
的数据只有"市本级及市辖区"一个合计行，而地图上该市有 3~5 个区多边形。

**采用的方案**（也是本系列的通用原则）：

- 把该市所有"区"多边形按同一个合计值**统一着色**，视作一个整体；
- 组内的区界保留成很淡的虚线（看得出是多个区），组的外边界另加重描边；
- 标注只在这一组的中心打一次，写"市本级及市辖区"；
- 面板上写明口径："市辖区按合计口径统一着色，不细分到区"。

**没有为了填满地图去编造各区的数。**

反过来的情况也要处理：南京有 11 个区各自有数，但另有一行"市本级"是财政层级、
**没有对应的地理多边形**。这一项不能塞进地图，也不能丢掉（丢了 11 个区之和就不等于全市），
处理方式是在地图外做一枚独立卡片并写明两者关系。

还有第三种：地图上存在、但数据里没有的单元。这种要么补齐数据，要么以中性色标注"未单列"
并在图例说明——**不能留空洞让人以为是渲染错误**。

---

## 8. 对接真实接口时的注意事项

当前版本数据全内联，改成接口驱动后会冒出一批新问题，都在前端这一侧：

### 8.1 DOM 必须先于 `echarts.init`，异步下更容易踩

§4.6 那条陷阱在异步场景下会加倍出现。数据回来之后如果先 `init` 再插入静态兄弟节点，
画布高度就锁死了。**建议：页面骨架（含所有静态兄弟节点）在数据到达之前就完整渲染出来，
数据只负责填内容和 `setOption`。**

### 8.2 分档边界必须按返回数据现算

现在多处分档（收入规模、同比增速、资金保障水平）的档位边界是按当期数据现算的，
不是写死的。接口化之后**尤其**不能写死——不同区间、不同层级的数据量级差异极大
（例如苏州的县区在百亿级、连云港在几十亿级，用同一套档位会让其中一个市全落进一档）。

分档算法建议保留现有做法：分位数切档 + 切点圆整到"好看的数" + 相邻切点过近时自动并档。

### 8.3 `registerMap` 与数据到达的时序

地图边界是静态资源，业务数据是动态的。**边界注册应当与数据请求解耦**：
边界就绪就先把底图画出来（中性色），数据到达后再 `setOption` 着色。
不要等数据回来才注册地图——那样首屏会有很长一段空白。

### 8.4 轮播与数据刷新的关系

常驻大屏需要定时刷新数据。要注意：

- 刷新**不应打断**当前轮播位置和用户手动选中的对象；
- 刷新时不要重新 `init` 图表，只 `setOption`；
- 翻牌数字在刷新时应从**当前值**缓动到新值，而不是从 0 重新数一遍；
- 请求失败时保留上一次的数据继续展示，并在页头标注数据时间，**不要清空成 0 或白屏**。

### 8.5 空态与异常态

每个面板都要想清楚三种状态：加载中 / 空数据 / 请求失败。常驻大屏上最糟的是
"看起来正常但其实是空的"——例如所有柱子都是 0 而没有任何提示。
建议空数据时在面板内居中显示明确文案，并让 §4.3 那条告警条同时点亮。

### 8.6 前端侧仍值得保留的两个习惯

虽然数值准确性由后端保障，但这两条是渲染层的纪律，建议保留：

- **合计由分项现算，不要在前端再存一份**。同一个数在前端出现两次就会有对不上的一天。
  若接口同时返回了分项和合计，用于**校验**而不是各显示各的。
- **保留自检与告警条**。前端至少断言"分项之和 ≈ 接口给的合计""层级条目与地图多边形一一对应"，
  不一致时点亮告警条。这不是替后端把关，而是让展示层的问题当场可见。

---

## 9. 已定型的决策与理由（"别改回去"清单）

以下每一条都是踩过坑之后定下来的，改回去会重现问题。

| 决策 | 理由 |
|---|---|
| **图表 `init` 后不调 `resize()`** | 整台靠 CSS transform 缩放，容器像素尺寸恒定，`resize` 无意义且会引发抖动 |
| **市辖区按合计口径统一着色，不细分到区** | 数据只有一个合计行，细分即编造 |
| **不做没有预算数支撑的"序时进度"列** | 缺少该层级的预算，硬造会与其他页对不上；改用可推导的"拉动 pct" |
| **仪表盘读数放弧下开口、刻度值放弧外** | 读数放圆心会被指针和转轴穿过；`axisLabel.distance` **正值是朝弧内**偏移，会把刻度压在色带上 |
| **分档着色只在同一档位色内做透明度渐变** | 渐变到别的色相时，又小又扁的地块整块只落在渐变尾段，同档不同色，分档着色即失效 |
| **翻牌缓动按时间推进，不按帧步进** | 按帧步进在主线程繁忙或标签页降频时步进次数不够，轮换到下一个对象时读数还停在中途——头条数字会长期是错的 |
| **翻牌用 `setInterval` 驱动，不用 `requestAnimationFrame`** | rAF 在页面不可见时整个停摆，读数会永久冻在半路 |
| **不用 `visualMap`，着色写在 `itemStyle`** | 见 §7.4 的两个陷阱 |
| **折线用 `showSymbol: false`，不用 `symbol: "none"`** | 见 §10 |
| **hash 用 `replaceState` 同步，不用 `location.hash =`** | 后者触发跳转与滚动，大屏上表现为抖动 |
| **土地出让 / 资金余额分析保留偏离主色系的告警色调** | 刻意制造轮播中的情绪节奏，不是配色错误 |
| **全文只允许一处外链（ECharts CDN）** | 保证自包含、可离线拷贝、便于审计 |

---

## 10. 踩坑记录

按"排查难度"从高到低排列。前三条都属于**报错信息完全不指向真实原因**的类型。

### 10.1 `symbol: "none"` 让 markPoint / markArea / markLine 崩溃

在 ECharts 6 里，折线系列如果写 `symbol: "none"`，再配 `markPoint` / `markArea` / `markLine`，
会在渲染阶段抛 `TypeError: Cannot read properties of undefined (reading 'coord')`。

**为什么难查**：这个异常发生在 `setOption` 内部，会中断整个外层函数，导致后面的
`setOption` 调用全都不执行。表现出来是"一张图画了一半、另一张图完全空白"，
而控制台只有一句**无栈的 `Script error.`**。

**解法**：用 `showSymbol: false` 代替 `symbol: "none"`。
**排查技巧**：把每个 `setOption` 用 try/catch 包起来，把 `e.stack` 存到 window 上再读——
这是拿到真实堆栈的唯一办法。

### 10.2 `aspectScale` 导致地图被静默裁掉

见 §7.2。没有任何报错，只是地图上下被切掉。

### 10.3 分段式 `visualMap` 的两个陷阱

见 §7.4。一个抛与 10.1 相同的无栈错误，一个静默吞掉图例标签。

### 10.4 `flex:1` 的图表容器高度锁死

见 §4.6。本系列栽过四次，是出现频率最高的一条。

### 10.5 翻牌缓动的中途值

按帧步进的缓动在主线程繁忙时步进不足，**验证截图时看到的数字可能是缓动中途值**。
两层含义：
- 验证时要等收敛后再判断，别把中途值当 bug；
- 但如果轮换周期比收敛还快，那就**真的是 bug**——读数几乎永远是错的。实测出现过
  目标 440 停在 465、目标 380 停在 404 的情况。

### 10.6 `emphasis` 不深合并 `rich` 样式

给地图/图表标签配了富文本样式时，`emphasis.label` 不会深合并 normal 态的 `rich` 定义，
高亮时标签会渲染成残缺的原始串（形如 `{n|无锡}`）。
**解法**：用一个工厂函数生成 label，normal 与 emphasis 各持一份完整定义。
同理 `emphasis.itemStyle` 如果不给 `areaColor`，ECharts 的默认强调色会把分档口径盖掉。

### 10.7 动态排序柱状图（bar race）的数据项不能带 `name`

在类目轴上，数据项带 `name` 会把它钉死在对应类目上，与 `realtimeSort` 的位置重排冲突，
表现为排序错乱。**数据项只带 `value` 与 `itemStyle`。**

### 10.8 树图 `upperLabel.formatter` 会作用到不可见的根节点

导致出现 `NaN 亿元 · NaN%` 的标签。**加空值保护，根节点返回空串。**

### 10.9 `formatter: "{c}%"` 会吞掉小数

`76.0` 会显示成 `76`。**一律用函数 formatter。**

### 10.10 名称匹配：长名优先 vs 精确优先

下钻入口靠名称匹配定位对象，两种场景规则相反：

- **行级下钻**拿整行文本去找子串，必须**长名优先**——否则"制造业"会把
  "电力热力燃气及水生产供应业"这类含子串的名字截胡；
- **画布级下钻**拿到的就是节点名本身，此时长名优先**反而有害**——
  "增值税"是"土地增值税"的子串，点增值税会被截胡跳到土地增值税去。

**解法**：画布级匹配加一层"精确命中优先"，找不到精确匹配再退回子串匹配。

### 10.11 图形点击取名：`name` 还是 `seriesName`

折线/小多图的 `params.name` 是 x 轴类目（月份），只有 `seriesName` 才是系列名；
树图/环形图正好相反。绑定点击事件时要按图表类型决定取哪个。

### 10.12 `notMerge` 与图例勾选态

不同视图之间如果 `markPoint` / `markArea` 结构不同，合并会残留上一个视图的标注，
必须用 `setOption(option, true)`。代价是图例勾选态被重置——需要在重建前
从 `chart.getOption().legend[0].selected` 取回再下发。
只监听 `legendselectchanged` 事件不够，因为 `dispatchAction` 触发的变更不走该事件。

---

## 11. 扩展指南

### 11.1 新增一块专题页

1. **选前缀**：确认两字母前缀未被占用（见附录）。
2. **抄骨架**：复制一块结构最接近的现有页作为基准，替换前缀。地图页优先参考已经处理过
   南海拆图与布局现算的那两块（全国分地区收入、四大板块分省明细）。
3. **定叙事**：先想清这一页要说的一句话，据此定主视觉，再配佐证面板。
4. **排高度**：把每一块的像素高度加起来，必须精确等于 1080。
5. **落 DOM 再 init**：所有静态节点先渲染，再创建图表实例。
6. **接导航**：在轮播配置表里加入这一页（决定它在轮播序列中的位置）；
   在门户页的导航宫格数据里加一张卡。
7. **重建索引**：跑索引构建脚本，确认分类与计数正确。

### 11.2 新增一块子页

1. **选原型**：按 §6.2 的判断顺序定版式。
2. **选前缀**：同上。
3. **抄子页骨架**：三级面包屑（含可点的父页链接）、返回按钮、对象切换器、时钟，
   这几样所有子页统一，直接复制。
4. **定 hash 规则**：明确 hash 里放什么（对象名、视图名），并用 `replaceState` 同步。
5. **排最紧的一档**：按对象里分项最多的那一个排版（例如西部 12 省、南京 11 区）。
6. **接入口**：在下钻配置表里登记「父页 → 子页」，选择行级 / 画布级 / 面板级；
   更新父页页头的子页导航标签；在门户页对应卡片上加"可下钻"角标。

### 11.3 新增一个下钻入口

只改注入脚本的配置表，**不要动页面源码**。需要提供：

- 父页文件名、入口的 DOM 选择器或图表容器 id
- 对象名列表（用于匹配）
- 若图上显示的是简称而 hash 需要全称，额外提供"图上名 → 跳转名"的映射
- 目标子页文件名

改完重新运行注入脚本即可（幂等，可反复跑）。

---

## 12. 验证清单

没有自动化测试。每次改动后按下面这份清单人工过一遍。

### 静态检查

- [ ] 抽出内联脚本跑一次语法检查
- [ ] 全文外链数 = 1（只有 ECharts CDN）
- [ ] `chart.resize(` 调用数 = 0
- [ ] 非本页前缀的 class / id / CSS 变量 / keyframes 数 = 0（`is-*` 除外）
- [ ] 没有 `<table>`、没有 `scrollTop`、没有 `symbol:"none"`、没有 `location.hash =`

### 浏览器目检

务必用 HTTP 服务打开，**不要用 `file://`**——地图内联数据大、部分浏览器会拦。

- [ ] 控制台零报错（注意 `Script error.` 这种无栈报错，见 §10.1）
- [ ] 窗口缩放时整台等比缩放，无变形、无滚动条
- [ ] 页面 `scrollWidth × scrollHeight` 恒为 1920×1080（无溢出）
- [ ] 告警条未被点亮
- [ ] 翻牌数字**收敛后**取值正确（别把缓动中途值当 bug，也别放过永不收敛的情况）

### 地图专用

- [ ] 用 `convertToPixel` 打极点，确认全部落在画布内（§7.2）
- [ ] 每个可切换对象逐一验（13 个市的包围盒差异极大，验一个不够）
- [ ] 数据条目与地图多边形一一对应，无多无少

### 交互

- [ ] 每个下钻入口点击后跳转正确、hash 定位正确
- [ ] 子页的每个对象 / 每个视图切换后都不溢出（按最紧的一档验）
- [ ] 轮播、暂停、上一页/下一页均正常
- [ ] 鼠标移入应暂停的面板时，自动轮换确实停了

---

## 13. 附录：27 块屏与 CSS 前缀对照

前缀是全仓唯一的，新建页面前先确认未被占用。

### 门户

| 前缀 | 页面 |
|---|---|
| `gl` | 公共资金专题总览 |

### 13 块专题页（轮播顺序）

| 序 | 前缀 | 页面 | 有无子页 |
|---|---|---|---|
| 1 | `nr` | 全国收入总览 | — |
| 2 | `gs` | 全国分地区收入 | 有 |
| 3 | `js` | 江苏地市收入 | 有（两块） |
| 4 | `xm` | 收入分项目 | 有（两块） |
| 5 | `zd` | 重点城市及县区 | 有 |
| 6 | `hy` | 税收分行业 | 有 |
| 7 | `xd` | 新动能税收 | 有（两块） |
| 8 | `td` | 土地出让形势 | 有 |
| 9 | `gz` | 国资社保预算 | — |
| 10 | `xj` | 现金管理 | 有 |
| 11 | `zs` | 资金运行指数 | — |
| 12 | `zq` | 政府债券发行 | 有 |
| 13 | `kc` | 资金余额分析 | 有 |

### 13 块子页

| 前缀 | 页面 | 父页 |
|---|---|---|
| `db` | 四大板块分省明细 | 全国分地区收入 |
| `sy` | 地市收入详情 | 江苏地市收入 |
| `bq` | 三大板块地市分布 | 江苏地市收入 |
| `xp` | 分项目收入明细 | 收入分项目 |
| `ym` | 主体税种月度走势 | 收入分项目 |
| `sx` | 重点县区详情 | 重点城市及县区 |
| `sh` | 行业税收详情 | 税收分行业 |
| `nd` | 新动能四大类地市分布 | 新动能税收 |
| `sl` | 新动能赛道地市分布 | 新动能税收 |
| `st` | 土地成交台账 | 土地出让形势 |
| `sj` | 现金操作详情 | 现金管理 |
| `sz` | 债券发行台账 | 政府债券发行 |
| `sn` | 资金余额预警详情 | 资金余额分析 |

### 体积说明

多数页面在 40~80 KB。内联了地理边界的页面明显更大：内联全国边界的约 530 KB，
内联江苏 13 市边界的约 185 KB，内联 95 个县区边界的约 490 KB。
接口化改造时，**地理边界建议改为独立的静态资源按需加载并缓存**，不必再内联进每个页面。
